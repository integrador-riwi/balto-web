# Balto APK download

Place the Android release build here when you want the landing page to serve it locally:

```txt
public/downloads/balto.apk
```

The landing page reads the download URL from `NEXT_PUBLIC_APK_URL`.
